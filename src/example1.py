"""Example Nr. 1."""
from __future__ import annotations

# def square(num: int) -> int:
#     """Calculate the square of the given integer."""
#     result: int = num * num
#     return result


def square(num: int) -> int:
    """Calculate the square of the given integer."""
    result: int = num * num  # caluclate the square and store it in result
    return result
