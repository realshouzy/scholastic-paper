"""Example Nr. 1."""
from __future__ import annotations

# pylint: skip-file


def square(num: int) -> int:
    """Calculate the square of the given integer."""
    # result: int = (num * num)
    result: int = num * num
    return result
